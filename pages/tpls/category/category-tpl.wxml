<template name="categorytop">
  <view class="category-detail-box">   
    <!--加载categorybanner和标题 --> 
    <view class="category-header">
      <image src='{{topInfo.url}}' mode="aspectFill"></image>
    </view>
    <view class="category-title">
      <text>{{topInfo.name}}</text>
    </view>


    <!-- <view class="category-main">
      <block wx:for="{{categoryInfo.products}}">
        <view class="category-goods-items {{index%3==1?'middle':''}}" data-id="{{item.id}}" bindtap="onProductsItemTap">
          <image src='{{baseImageUrl}}{{item.main_img_url}}' mode="aspectFill"></image>
          <view>{{item.name}}</view>
        </view>
      </block>
    </view> -->
  </view>
</template>